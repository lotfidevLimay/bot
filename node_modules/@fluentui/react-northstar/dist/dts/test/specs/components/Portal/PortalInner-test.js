"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var React = require("react");
var portalContext_1 = require("src/components/Provider/portalContext");
var PortalInner_1 = require("src/components/Portal/PortalInner");
var utils_1 = require("test/utils");
var mountPortalInner = function (props) {
    return utils_1.mountWithProvider(React.createElement(PortalInner_1.PortalInner, __assign({}, props),
        React.createElement("p", null)));
};
describe('PortalInner', function () {
    describe('render', function () {
        it('calls react createPortal', function () {
            var mountNode = document.createElement('div');
            var comp = mountPortalInner({ mountNode: mountNode });
            expect(mountNode.contains(comp.getDOMNode())).toBeTruthy();
        });
    });
    describe('onMount', function () {
        it('called when mounting', function () {
            var onMount = jest.fn();
            mountPortalInner({ onMount: onMount });
            expect(onMount).toHaveBeenCalledTimes(1);
        });
    });
    describe('onUnmount', function () {
        it('is called only once when unmounting', function () {
            var onUnmount = jest.fn();
            var wrapper = mountPortalInner({ onUnmount: onUnmount });
            wrapper.unmount();
            expect(onUnmount).toHaveBeenCalledTimes(1);
        });
    });
    describe('document.body', function () {
        it('adds an element to document.body', function () {
            var className = 'a-sample-classname';
            var wrapper = utils_1.mountWithProvider(React.createElement(portalContext_1.PortalContext.Provider, { value: { className: className } },
                React.createElement(PortalInner_1.PortalInner, null,
                    React.createElement("div", null))));
            expect(document.querySelector("." + className)).toBeInTheDocument();
            // element should be removed on unmount
            wrapper.unmount();
            expect(document.querySelector("." + className)).not.toBeInTheDocument();
        });
        it('reacts on "className" update and keeps node in HTML tree', function () {
            var className = 'a-sample-classname';
            var wrapper = utils_1.mountWithProvider(React.createElement(portalContext_1.PortalContext.Provider, { value: { className: className } },
                React.createElement(PortalInner_1.PortalInner, null,
                    React.createElement("div", { id: "sample" }))));
            expect(document.querySelector("." + className)).toBeInTheDocument();
            expect(document.querySelector("." + className + " #sample")).toBeInTheDocument();
            var newClassName = 'an-another-classname';
            wrapper.setProps({ value: { className: newClassName } });
            expect(document.querySelector("." + className)).not.toBeInTheDocument();
            expect(document.querySelector("." + newClassName)).toBeInTheDocument();
            expect(document.querySelector("." + newClassName + " #sample")).toBeInTheDocument();
        });
    });
});
