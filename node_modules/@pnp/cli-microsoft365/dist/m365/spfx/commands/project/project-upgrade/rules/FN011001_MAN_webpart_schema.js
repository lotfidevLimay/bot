"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.FN011001_MAN_webpart_schema = void 0;
const ManifestRule_1 = require("./ManifestRule");
class FN011001_MAN_webpart_schema extends ManifestRule_1.ManifestRule {
    constructor(schema) {
        super();
        this.schema = schema;
    }
    get id() {
        return 'FN011001';
    }
    get title() {
        return 'Web part manifest schema';
    }
    get description() {
        return 'Update schema in manifest';
    }
    get resolution() {
        return `{
  "$schema": "${this.schema}"
}`;
    }
    get severity() {
        return 'Required';
    }
    visit(project, findings) {
        if (!project.manifests ||
            project.manifests.length === 0) {
            return;
        }
        const occurrences = [];
        project.manifests.forEach(manifest => {
            if (manifest.componentType === 'WebPart' &&
                manifest.$schema !== this.schema) {
                const node = this.getAstNodeFromFile(manifest, '$schema');
                this.addOccurrence(this.resolution, manifest.path, project.path, node, occurrences);
            }
        });
        if (occurrences.length > 0) {
            this.addFindingWithOccurrences(occurrences, findings);
        }
    }
}
exports.FN011001_MAN_webpart_schema = FN011001_MAN_webpart_schema;
//# sourceMappingURL=FN011001_MAN_webpart_schema.js.map